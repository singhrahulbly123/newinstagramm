import type { Metadata } from 'next';
import InstallAppClient from './InstallAppClient';
export const metadata: Metadata = { title: 'Install globltools App', description: 'Install globltools on your phone or computer for faster access to media utilities.', alternates: { canonical: '/install-app' } };
export default function Page() { return <main className="mx-auto max-w-4xl px-4 pb-6 pt-12 sm:px-6"><p className="text-sm font-bold uppercase tracking-[0.2em] text-emerald-700">Installable web app</p><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-5xl">Keep globltools one tap away</h1><p className="mt-4 max-w-2xl leading-7 text-slate-600">Install the website like an app without an app-store download.</p><div className="mt-8"><InstallAppClient /></div><section className="mt-8 grid gap-4 sm:grid-cols-3">{['Android: use Chrome and tap Install app.','iPhone: tap Share, then Add to Home Screen.','Desktop: use the address-bar install icon.'].map((text, index) => <article key={text} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="font-black text-emerald-700">0{index + 1}</p><p className="mt-3 text-sm leading-6 text-slate-600">{text}</p></article>)}</section></main>; }
